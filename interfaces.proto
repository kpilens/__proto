syntax = "proto3";
package interfaces;

message PaginateOptions {
  oneof limit {
    string limitString = 1;
    int32 limitInteger = 2;
  }
  oneof page {
    string pageString = 3;
    int64 pageInteger = 4;
  };
}
