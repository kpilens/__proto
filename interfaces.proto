syntax = "proto3";
package interfaces;

message Limit {
  oneof limit {
    double number_value = 1;
    string string_value = 2;
  }
}

message Page {
  oneof page {
    double number_value = 1;
    string string_value = 2;
  }
}

message PaginateOptions {
  Limit limit = 1;
  Page page = 2;
}
